export enum InvoiceStatus {
  InProgress,
  Archived,
  Completed
}

export enum ShipmentStatus {
  Standard,
  Hot
}
export enum InvoiceMode {
  FCL,
  LCL
}

export enum DevanningEnum {
  No,
  Full,
  Partial
}
